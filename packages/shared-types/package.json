{
  "name": "@legal-recovery/shared-types",
  "version": "1.0.0",
  "description": "Shared types and DTOs for Legal Recovery OS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "zod": "^3.24.0"
  }
}
