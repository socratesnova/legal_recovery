/** BullMQ queue name for the async portfolio ingest pipeline. */
export const PORTFOLIO_INGEST_QUEUE = "portfolio-ingest";

/** BullMQ job name within the queue (a queue may host several job types). */
export const PORTFOLIO_INGEST_JOB_NAME = "ingest";
